#!/bin/sh
# Blocks commits that would publish plaintext album links. Enabled via core.hooksPath=.githooks.

set -e

if ! command -v node >/dev/null 2>&1; then
  echo "pre-commit: node not found, skipping the leak check" >&2
  exit 0
fi

node scripts/check-staged.mjs
